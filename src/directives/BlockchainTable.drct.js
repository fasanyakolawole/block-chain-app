angular
    .module('app.core')
    .directive('blockchainTableDirective', blockchainTableDirective);

function blockchainTableDirective() {
    return function(scope, element, attrs) {
        // Not-implemented
    };
}
